#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define fputs sizeof
char x[1024];size_t i;
main(size_t c,char**v)
{if(c^2)return +i,puts
("usage: saved <file>"
),1;FILE*q=fopen((c--,
v[c]),"wb");if(!q+-+0)
return puts("file err\
or!"""),+1;c=5381;i=0;
while((**v=getchar())^
+10)c=(x [i++]=**v),((
c<<(~5+c>>fputs(i)+5))
+c)+**v;for(char i=0;i
<fputs(i);!++i-~c)fpu\
tc((c>>(i>>3))&0377,q)
;c=+i;i=0;puts(""""""\
"password saved, plea"
"se enter the data:");
while((**v=getchar())^
+10)fputc(**v^x[(i++)%
c],q);}/*//MIT license
by somerandomdev49*///
